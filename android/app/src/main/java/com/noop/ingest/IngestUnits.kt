package com.noop.ingest

/** Unit conversions shared by the file importers, so a CSV in imperial units converts one way. */
internal object IngestUnits {

    /** Pounds to kilograms (exact avoirdupois definition). */
    const val LB_TO_KG = 0.45359237
}
