---
whatsnew:
  title: "Truer HRV, blood oxygen that stops guessing, and Health Connect both ways"
  date: "July 2026"
  items:
    - "**Your HRV was reading low.** When several heartbeats land in the same second, they were being read shortest-first instead of in the order they happened — and HRV is built entirely from the gaps between consecutive beats, so sorting them flattened it. On this fork's own strap the nightly figure was about a quarter too low. Nights recorded from here on read true; earlier nights keep their stored values, because the original order was never saved."
    - "**Blood oxygen on WHOOP 4.0 is no longer shown.** The reading it produced was far too low to be real. The sensor pair is sampled once a second, which is slower than a heartbeat, so the pulse the calculation depends on never reaches the app. Rather than publish a number we know is wrong, the app now says nothing. WHOOP 5.0 and MG are unaffected — that reading comes from the strap itself."
    - "**Skin temperature now reaches Health Connect**, along with your HRV, VO2 max and heart rate. Almost nothing else on a phone measures skin temperature, so it is the most useful thing NOOP can contribute."
    - "**Health Connect fills more in for you.** Weight and height update your profile, so the scores that depend on them stop drifting. Blood pressure from a paired cuff, hydration, and nutrition all arrive on their own, and period dates now feed cycle tracking, which had no source for them before."
    - "**The app is built on a current toolchain** and targets Android 16."
---
HRV correctness, an honest answer on WHOOP 4.0 blood oxygen, and Health Connect working properly in
both directions.

## Fixed

- **HRV was under-reported.** R-R beats sharing a second were read in magnitude order rather than the
  order the strap emitted them. RMSSD is built from the difference between consecutive beats and is
  computed within a second, so that ordering is its entire input — and sorting shrinks adjacent
  differences by construction. Measured across five straps: 25% low on this fork's own 5.0 (23.5 ms
  against 29.3), 17% on another user's. The effect scales with how often a second carries three or
  more beats; a two-beat second is unaffected. Beats are now stamped with their emission order at
  decode. Nights recorded before this keep their stored values — the order was never recorded and
  cannot be recovered, so nothing is silently rewritten.
- **WHOOP 4.0 blood oxygen is withheld.** The red/IR pair arrives once a second, so the fastest signal
  it can represent is 0.5 Hz while a pulse runs 0.83 to 3 Hz: the pulsatile component the calculation
  reads is gone before decode. Confirmed on two straps and 2.1 million samples — 89% and 98% of
  windows carried no pulsation, and the survivors produced ~80%, which would be severe hypoxaemia in
  two healthy people. It also no longer leaves the device through Health Connect. The 5.0/MG value is
  the strap's own and is unaffected.
- **The raw sensor export produced an empty file** for any strap paired through the wizard: it asked
  for the legacy device id instead of the active one. Its window also covered 24 hours against an
  offload carrying about a fortnight.
- **Backups from different builds could delete each other.** Snapshots from the debug and mock builds
  were named identically to the release app's, and pruning counted them as one pool.

## Added

- **Health Connect, outbound:** skin temperature (absolute reading plus your deviation from your own
  norm) and VO2 max, alongside the heart rate, HRV, resting HR, respiratory rate, sleep and workouts
  already written.
- **Health Connect, inbound:** weight and height now update your profile, so BMI, BMR and the VO2 max
  estimate stop working from a figure typed in months ago. Blood pressure from a paired cuff,
  hydration and nutrition arrive on their own. Period start dates feed the cycle classifier, which
  had the parameter but never a source.
- **Raw frame capture works on WHOOP 4.0**, not only 5.0/MG.

## Under the hood

Gradle 9.6.1, AGP 9.3.1, Kotlin 2.3.10, compileSdk 36, Room 2.8.4, Health Connect 1.1.0.

**Every per-second field the strap sends is now kept.** The storage funnel decoded thirteen channels
and discarded whatever it had no column for, and the strap erases its own backlog the moment an
offload is acknowledged, so those bytes were gone for good. They now land in the database: two
further thermal registers beside the skin one, the strap's own record counter, the optical
front-end telemetry including the flag marking a second whose beat detection the strap could not
trust, and four channels whose meaning is not established, stored under names that claim nothing.
Nothing reads them yet — the point is that a question asked later can be answered from history
instead of needing another capture.

The database moves to v101, carrying both the beat-order column and the per-second channels in one
additive nullable step. The earlier form of that step was verified on a real phone by installing
v100, seeding it, then upgrading in place — 1,456 rows across ten tables, all preserved, integrity
clean, no crash. It has grown since and wants re-running before release.

Releases are now assembled by one generator from one notes file per version, so a candidate and its
stable read the same text.

## With thanks

The HRV ordering fix follows **@ryanbr**'s [#823](https://github.com/ryanbr/noop/issues/823) and
[#830](https://github.com/ryanbr/noop/pull/830) in upstream [ryanbr/noop](https://github.com/ryanbr/noop),
which identified the magnitude-sort and the `ord`-at-decode shape of the fix. Verified here against
five straps before adopting.

The v18 optical channel corrections follow **@vishk23**'s
[#861](https://github.com/ryanbr/noop/pull/861), and banking the discarded per-second fields follows
their [#848](https://github.com/ryanbr/noop/pull/848) and the field inventory in
[#845](https://github.com/ryanbr/noop/issues/845). Checked against our own captures first: the
"higher-precision heart rate" those notes describe at offset 36 is two unrelated bytes, and on a
second strap it reads 227 bpm against a recorded 63.

## Known limits

- **WHOOP 4.0 support is decoded and now exercised on real hardware**, but only lightly: one strap,
  four days.
- **Rhythm Age needs about seven days of continuous wear** before it produces anything.
- Historical nights keep the HRV values they were stored with.
